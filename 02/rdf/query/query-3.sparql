PREFIX ex: <http://example.org/vocabulary/>
PREFIX dbo: <http://dbpedia.org/resource/classes#>

# select houses where some mayor lives  

SELECT ?house
WHERE {
  ?mayor a ex:Mayor .
  ?mayor ex:livesIn ?house .
}
